{
  "name": "quality-tracker-backend",
  "version": "1.0.0",
  "description": "Quality Tracker Webhook Backend Server",
  "main": "webhook-server.js",
  "scripts": {
    "start": "node webhook-server.js",
    "dev": "nodemon webhook-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["quality", "tracker", "webhook", "testing"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.5.0",
    "express": "^4.21.1",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}